# 暗記カード製造マシン 仕様書 v08

プレフィックス付きテキストを、暗記カード（Anki用）の4項目「問題・回答・出典・タグ」へ変換するツールの仕様書。他のAIへ本ツールの構文と制約を説明する目的で使用する。

**このファイルの責務は、技術的な制約だけである。** カードに何を書くか（学習上の設計）は定めない。それは `question-prompt` 側の責務。

**判定基準：その規則の違反をツールが検知できるなら、このファイルに書く。検知できないなら書かない。**

---

## 1. 概要

- 入力：テキスト（1行＝1項目、行頭のプレフィックスが役割を示す）
- 出力：Anki取り込み用のTSVファイル
- カードは「問題・回答・出典・タグ」の4項目を持つ

---

## 2. 入力形式

### 2-1. プレフィックス

各行の先頭に **英字1文字＋半角スペース1つ** を置く。それ以降が本文。

| 記号 | 役割 | 必須 | 1カード内の個数 |
|------|------|------|----------------|
| `Q` | 問題（表面） | 必須 | 1個のみ |
| `A` | 回答（裏面） | 必須 | 1個以上 |
| `S` | 出典 | 任意 | 0または1個 |
| `T` | タグ | 任意 | 0または1個 |

### 2-2. カードの境界

**`Q` 行の出現が、新しいカードの開始を意味する。**

空行は無視される。空行の有無で結果は変わらない。

### 2-3. 記述例

```text
Q 図2のエミッタ抵抗帰還増幅回路を、図3の低周波交流等価回路へ変換する手順を示せ。
A ❶直流電源\(V_{CC}\)を信号に対して接地する
A ❷\(V_{CC}\)が短絡なので\(R_1\)と\(R_2\)は並列\(R_1 \parallel R_2\)になる
A ❸トランジスタを\(r_{ie}\)と電流源\(\beta_0 i_b\)に置換する
S PDF p55 通信工学1 問題1
T 通信工学
```

### 2-4. 本文に書ける文字

**本文にはあらゆる文字を制限なく使用できる。**

行頭のプレフィックスだけが構文解析の対象であり、それ以降の文字は区切りとして解釈されない。`;` `|` `\` `:` `{}` `出典：` などをそのまま書いてよく、置換やエスケープは不要。

唯一の構文上の制約は、**1つの項目を1行に収めること**である。

---

## 3. 解析

1. 改行で行に分ける。空行はスキップする。
2. 各行の先頭2文字を見る。英字1文字＋半角スペースでなければエラー（第6章）。
3. `Q` 行が現れたら、新しいカードを開始する。
4. 3文字目以降を本文とし、前後の空白を除去してプレフィックスごとに格納する。半角スペースが2個以上続いても、除去されるだけで正常に扱う。
5. `T` の本文を半角・全角スペースで分割し、複数タグとする。

---

## 4. 出力

### 4-1. 変換

| 出力項目 | 生成方法 |
|---------|---------|
| 表面 | `Q` の本文。**小問の境界に `<br>` を挿入する（下記）** |
| 裏面 | `A` 行を出現順に `<br>` で連結し、`S` があれば末尾に出典表記を付加 |
| タグ | `T` の本文をスペースで分割 |

**表面の小問改行:**

`Q` の入力は1行のまま保持し、**出力時にだけ**小問の境界へ `<br>` を挿入する。挿入するのは、次を**すべて**満たす位置に限る。

> 1. 小問記号が **`\( ... \)` `\[ ... \]` の内側でない**
> 2. 小問記号の**直前**が **`。` ／ 半角・全角スペース ／ `）` ／ `)`** のいずれか
>    - 直前がスペースのときは、**その空白列の先頭で切り、空白は次の行に持ち越さない**
>    - ただし **`\)`（数式の閉じ）の直後は、括弧付き番号のときだけ許す。丸数字は許さない**
> 3. 直前が**小問記号そのものでない**

- 小問記号は `(1)`〜`(99)`、`（1）`〜`（99）`（全角数字も可）、`①`〜`⑮`、`（ア）`〜`（ン）`
- **挿入は HTMLエスケープ（4-2）の後に行う。** 先に入れるとエスケープされて `&lt;br&gt;` になる
- 入力データも TSV の列構造も変わらない。`Q` は1カード1個・1行のまま（2-1・2-4）

**各条件が止めているもの（2026-08 実測・実運用カード100枚）:**

| 条件 | 止めている型 | 実例 |
|---|---|---|
| 2の「`\)` は括弧付き番号のみ」 | **式番号への参照** | `…\sin(\omega_c t)\)③を搬送波、上側波帯、下側波帯に分解せよ` |
| 3 | **小問番号への参照** | `…に分解せよ。(2)(1)より信号波成分と…` ／ `…を求めよ。(3)(1)、(2)より…` |
| 2（読点を含めない） | **範囲表現・列挙** | `その過程を、①から⑤の各波形図を用いて` ／ `熱化学方程式①，②，③を用いて` |

**判定条件の選定（2026-08 実測・同100枚）:**

| 案 | 発火 | 分割点 | 誤分割 | 判定 |
|---|---:|---:|---|---|
| 句点の直後だけ（v07） | 30枚 | 59 | 0 | 取りこぼしが大きい。半角スペース区切りの小問と、括弧・選択肢の型を全部落とす |
| 小問記号の直前すべて | 36枚 | 109 | あり | 式番号・範囲表現まで割る |
| `(N)` `（N）` のみ・全位置 | 17枚 | 59 | 4件 | 参照の `(1)` を割る |
| 読点（`、` `，`）を境界に含める | ― | +10 | 9件 | 正1件に対し誤9件 |
| 直後の助詞で参照を除外する | ― | ― | 0 | 誤分割は消えるが、`(3)より AM 信号の全電力…` のような正しい小問まで落とす |
| **上記1〜3（採用）** | **32枚** | **95** | **0件** | 約物だけで判定し、日本語の語彙判定を持ち込まない |

**採用した条件では、95箇所すべてを1件ずつ照合して誤分割0件を確認している。** 既知の取りこぼしは0件。

出典表記の付加は、裏面末尾に次を追加する形で行う。

```html
<br><span style="font-size:0.8em;color:#888">出典：{S の本文}</span>
```

**入力データに `出典：` と書く必要はない。** ツールが表示時に付ける。

### 4-2. HTMLエスケープ

Ankiは `#html:true` で取り込まれた本文をHTMLとして解釈する。エスケープしない場合、`<...>` はタグとして消え、`&...;` は実体参照に変換される。

**したがって、本文の `&` `<` `>` を次の順序でエスケープする。**

1. `&` → `&amp;`
2. `<` → `&lt;`
3. `>` → `&gt;`

順序を守ること。`&` を最後にすると二重変換が起きる。

**ツールが挿入する `<br>` と出典用の `<span>` はエスケープしない。** エスケープは本文に対して行い、その後に連結する。

この処理により、MathJax内の不等号（`\(a < b\)`）も、本文中の記号（`AT&T`、`A <-> B`）も、正しく表示される。副作用として、HTML実体参照（`&deg;` など）は文字列のまま表示される。度記号は `\(^\circ\)` で書けるため実害はない。

### 4-3. TSVの構造

3列（表面・裏面・タグ）をタブ区切りで出力する。先頭に取り込み設定を置く。

```text
#separator:tab
#html:true
#notetype:Basic-AnkiGen
#deck:{デッキ名}
#tags column:3
```

- ノートタイプは `Basic-AnkiGen` に固定する。Anki上の名称と完全一致していること
- デッキ名は「名前＋日時」形式で自動生成する。名前は最頻タグ、無ければ `暗記カード`
- デッキ名の半角コロンは `-` に置換する（`::` はAnkiが階層区切りとして解釈するため）
- 本文中のタブは半角スペースに置換する

CSVは使用しない。LaTeXにカンマが多くエスケープが発生するため、TSVを用いる。

### 4-4. 保存形式

| 項目 | 値 |
|------|-----|
| 拡張子 | `.tsv` |
| MIMEタイプ | `text/tab-separated-values;charset=utf-8` |
| 文字コード | UTF-8、先頭にBOM（`\uFEFF`）を付ける |
| ファイル名 | デッキ名と同一（`<デッキ名>.tsv`） |

**要件：保存したファイルを、名前を変えずにそのままAnkiへ取り込めること。** PC・タブレットのいずれの環境でもこれを満たす。

保存方式は選択させない。**1方式に固定する。**

#### MIMEタイプの選定理由

内容がタブ区切りテキストであるため、それと一致する `text/tab-separated-values` を用いる。`application/octet-stream` は「解釈しないバイナリ」を意味し、実体と食い違う。ファイルを受け取る側が内容を推測できなくなるため、回避目的が無い限り用いない。

#### 実測記録（2026-08）

当初、iOS では Safari 以外のブラウザも WebKit を使うため、`text/plain` を指定すると拡張子の後ろに `.txt` が付加される（`deck.tsv` → `deck.tsv.txt`）という懸念があった。

これを確認するため、次の3方式を実機で比較した。

| 方式 | MacBook | iPad（Edge） |
|------|---------|-------------|
| `text/tab-separated-values` | `.tsv` で保存・取込可 | `.tsv` で保存・取込可 |
| `application/octet-stream` | 同上 | 同上 |
| `text/plain` | 同上 | 同上 |

**3方式とも `.txt` の付加は起きなかった。** 拡張子を `.tsv` と明示していれば、MIMEタイプに関わらず正しく保存される。

したがって切替機能は不要と判断し、内容と一致する方式に固定した。**将来 `.txt` が付加される事象が再発した場合は、上表の他方式を試す**（実装では1行の変更で済む）。

#### 環境ごとの確認

保存形式を変更したときは、次の両方で確認する。片方だけでは足りない。

| 環境 | 確認すること |
|------|------------|
| PC | ファイル名が `<デッキ名>.tsv` である |
| タブレット | 同上。`.txt` が付加されていない |

---

## 5. エラーと警告の区別

**判定原則：構文を解析できないものはエラー。解析はできるが内容に問題があるものは警告。**

| 区分 | 意味 | ツールの動作 |
|------|------|------------|
| エラー | その行の役割やカードの構造を確定できない | 行番号を報告し、**該当カードを生成しない** |
| 警告 | 構造は確定できるが、内容に問題がある | 行番号を報告し、**カードは生成する。本文は書き換えない** |

新しい検査項目を追加するときは、この原則で振り分ける。

---

## 6. エラー条件

次のいずれかに該当する行はエラーとし、**行番号を報告してカードを生成しない**。

| 条件 | 理由 |
|------|------|
| 行頭が英字1文字＋半角スペースでない | 役割を判定できない |
| `Q` `A` `S` `T` 以外のプレフィックス | 役割が未定義 |
| `Q` が無いまま `A` `S` `T` が出現 | 帰属先のカードが無い |
| `Q` はあるが `A` が1行も無い | 裏面が存在しない |
| `Q` または `A` の本文が空 | 内容が無い |
| 同一カード内に `Q` が2個以上 | 1枚に2つの問いがある |

プレフィックスは**大文字のみ**を認める。小文字は未定義として扱う。

**エラー行は前後の行に吸収されない。** 1件のエラーで処理全体を止めず、他のカードは生成する。

---

## 7. 貼付マーカー

図表を後から貼り付ける作業を管理するための記法。

### 7-1. 語彙

**次の3語だけを認める。** 内容の説明や位置情報を入れない。

| マーカー | 貼る面 | 作業内容 |
|---------|-------|---------|
| `【問題図貼付】` | 表 | 資料の図表を切り取って表面に貼る |
| `【解答図貼付】` | 裏 | 資料の解答図を切り取って裏面に貼る |
| `【作図貼付】` | 裏 | 自分で描いた図を裏面に貼る |

### 7-2. 配置

- すべて `Q` 本文の冒頭に置く
- 1カードにつき最大3個。**同種は1個まで**
- 複数ある場合の順序は上表のとおり（表面用が先）
- 裏面に貼るマーカーも `Q` 側に置く。`A` 側に置くと裏を見るまで気づかず貼り忘れる

### 7-3. 位置情報

**マーカーに位置情報を入れない。** どこを開くかは `S` の内容で分かる。

このため、**マーカーがあるカードでは `S` を書くべきである。** `S` が無いと貼付箇所を特定できないため、ツールは警告を出す（第10章）。ただし構文としては成立するため、カードは生成する。

### 7-4. 検査項目

ツールは次を機械的に検査し、**該当すれば警告を出す**（第5章の原則により、いずれも構文は解析できるためエラーにしない）。

- 3語以外の `【…貼付…】` 形式の文字列がないか
- 同種のマーカーが重複していないか
- 順序が 7-2 のとおりか
- マーカーがあるカードに `S` があるか

ツールはマーカーを並べ替えたり削除したりしない。

### 7-5. 運用

貼付後、マーカーを画像に置き換えるか削除する。Anki の本文検索 `"貼付"` で作業待ちを一括抽出できる。作業がマーカーの消去を兼ねるため、消し忘れが構造的に起きない。

---

## 8. 数式・科学表記（MathJax）

数式はAnki側で描画される。描画できない書式を入れると表示が壊れるため、以下を技術制約として扱う。`Q` `A` の両方に適用する。

### 8-1. 使用できる記法

| 記法 | 用途 |
|------|------|
| `\( ... \)` | インライン数式 |
| `\[ ... \]` | ディスプレイ数式（中央寄せ・別行立て） |

**どちらを使うかは学習上の判断であり、`question-prompt` 側で決める。** このファイルは両方が使用可能であることだけを定める。

### 8-2. MathJaxで書く対象

| 対象 | 例 |
|------|-----|
| 数式・関係式・不等式 | `\(E=h\nu\)`、`\(\omega_c-\omega_m\le\omega\)` |
| 化学反応式・熱化学方程式 | `\(\mathrm{Mg}+2\mathrm{H_2O}\to\mathrm{Mg(OH)_2}+\mathrm{H_2}\)` |
| 元素記号・化学式・イオン式・同位体 | `\(\mathrm{Fe_2O_3}\)`、`\(^{13}_{6}\mathrm{C}\)`、`\(\mathrm{Na^+}\)` |
| 物理量・変数記号 | `\(n\)`、`\(\lambda\)`、`\(m_f\)` |
| 電子配置・軌道表記 | `\(1s^2 2s^2 2p^2\)` |
| 数値＋単位 | `\(20\ \mathrm{g}\)`、`\(2.18\times10^{-18}\ \mathrm{J}\)`、`\(25\ ^\circ\mathrm{C}\)` |

### 8-3. MathJaxにしない対象（通常の文字で書く）

- 問題番号・小問番号（`1.` `①` `(1)` `【①】`）
- 日本語の文中にある個数・順序・桁数（「有効数字3桁」「次の2つ」）
- 単位を伴わない日本語表現の中の数
- 構造を持たない略号（LPF、BPF、VCO、BCC など）

### 8-4. 禁止

| 禁止 | 理由 |
|------|------|
| Unicode上下付き（`²` `³` `⁻` `⁰` `₁` `₂` `₃` など） | 後からAnkiで編集できない。判定規則が二重になる |
| `$ ... $`、`$$ ... $$` | Ankiで数式として解釈されない |
| 数式用HTMLタグ | 描画されない |
| 数式の内側の改行 | 1項目1行の構文に反する |

間隔が必要なら `\quad` `\qquad` `\,` または通常の空白を使う。度記号は `\(^\circ\)`。

### 8-5. 検査項目

ツールは次を機械的に検査し、**該当すれば警告を出す**（第5章の原則により、いずれも構文は解析できるためエラーにしない）。

- Unicode上下付き文字が含まれていないか
- `\(` と `\)`、`\[` と `\]`、`{` と `}` の個数が対応しているか
- `$` による数式記法が使われていないか

ツールは数式を書き換えない。

**符号・添字・単位を原資料から変えないことは意味上の要件であり、ツールでは検知できない。`question-prompt` 側の責務とする。**

---

## 9. タグ

- `T` の本文をスペース（半角・全角）で分割し、複数タグとする
- **タグ内に空白を含められない。** 語の中の空白はアンダースコアにする（例：`電気回路_基礎`）
- タグ内にHTMLタグ・MathJaxを入れない
- ツールは複数タグを受理する。**何個生成するかは `question-prompt` 側で決める**

---

## 10. 警告

次の場合に警告を表示する。**いずれもカードは生成し、本文は書き換えない。**

| 条件 | 意図 | 出典 |
|------|------|------|
| 表示量スコアが閾値以上 | 分割の検討を促す | 10-1 |
| `A` 行が7行を超える | 同上 | ― |
| 同一カード内に `S` が2個以上 | 最後の1つを採用したことを知らせる | ― |
| 同一カード内に `T` が2個以上 | 同上 | ― |
| 3語以外の `【…貼付…】` がある | 貼付作業の検索性が壊れる | 7-4 |
| 同種のマーカーが重複している | 同上 | 7-4 |
| マーカーの順序が 7-2 と異なる | 同上 | 7-4 |
| マーカーがあるが `S` が無い | 貼付箇所を特定できない | 7-3 |
| Unicode上下付き文字がある | 後からAnkiで編集できない | 8-5 |
| `\(` `\)` `\[` `\]` `{` `}` の個数が不一致 | 数式の描画が壊れる | 8-5 |
| `$` による数式記法がある | Ankiで数式として解釈されない | 8-4 |

### 10-1. 表示量スコア

入力文字数はMathJaxのコード長に左右され、実際の表示量と一致しない。そのため、Anki上の表示量を近似する指標を用いる。

算出対象は `A` 行の本文（`<br>` 挿入前・エスケープ前）。

1. `\( ... \)` および `\[ ... \]` の内側は、次の合計を数える
   - `\` に続く英字列（`\beta` `\parallel` など）を1
   - `{` `}` `_` `^` および空白を0
   - それ以外の文字を1
2. 数式の外側は、全角文字を2、半角文字を1として数える
3. 両者を合計する

**閾値は500以上で警告。**

#### 閾値の根拠

実測16枚の分布から決定した。

| 出所 | スコア |
|------|--------|
| 手書きの正解例 | 60〜196 |
| プロンプトの記載例 | 254、343 |
| 実出力（比較軸が揃ったカード） | 229、411、430 |
| 実出力（7観点を1枚に載せたカード） | 636 |
| 構造のない詰め込みカード（参照） | 556 |

411・430は比較軸が揃った完全な比較カードであり、内容を削ると比較が成立しなくなる。警告を出すべきではない。636は観点を絞る余地があり、556は構造がない。500はこの境界に位置し、16枚中2枚で発火する。

#### この指標の性質と限界

**このスコアは総量を測るものであり、読みやすさを測るものではない。**

入力形式が1行1カードだった時代は、総量がそのまま読みにくさに直結した。現在は `A` 行によって1項目1行が保証されるため、同じ総量でも構造があれば読める。実際、636のカードは7行に分かれており、構造のない556より読みやすい。

したがって警告は「読みにくい」ではなく「**このカードは大きい。見直す価値がある**」という意味に解釈する。

#### 警告への対処

`question-prompt` の分割原則により、**原文の問いが1つなら1枚**である。したがって警告が出ても分割で解決できない場合がある。

その場合の対処は分割ではなく、**比較軸や項目を絞ること**である。カードの目的は対象間の違いを覚えることであり、各対象の全性質を載せることではない。

#### 再調整

この値は16枚に基づく。運用を続けて分布が変われば見直す。ツールは全カードのスコアを一覧表示し、判断材料を提供する。

---

## 11. 入力例

### 例1：全項目あり

```text
Q 電源電圧\(E\)が固定で抵抗\(R\)が変化するとき、負荷線はどう変化するか。
A 傾き\(-1/R\)が変わり、\(V\)軸切片\(E\)は動かない
A \(R\)が大きいほど傾きは緩やかになる
S PDF p49 通信工学1 問題
T 通信工学
```

### 例2：出典・タグを省略

```text
Q 問題文
A 回答
```

### 例3：貼付マーカーあり

```text
Q 【問題図貼付】【作図貼付】左の回路について、負荷線の概形を描け。
A 傾き\(-1/R\)、\(V\)軸切片\(E\)、\(I\)軸切片\(E/R\)を通る直線
S PDF p49 通信工学1 問題
T 通信工学
```

マーカーがあるため `S` を書く。無い場合は警告が出るが、カードは生成される。

### 例4：複数カード（空行は任意）

```text
Q 問題A
A 回答A
T 科目

Q 問題B
A 回答B
T 科目
```

---

## 12. 版ごとの主な変更

### v07 → v08

| 項目 | v07 | v08 |
|------|-----|-----|
| 表面の改行条件 | 句点の直後の小問記号のみ（実測30枚59箇所） | **句点・スペース・閉じ括弧の直後（実測32枚95箇所・誤分割0）** |

v07 は誤分割0を優先した保守的な条件で、**実運用では必要な改行を取りこぼしていた。** 実測では、小問記号の直前が半角スペースの28件はすべて正しい境界であり、閉じ括弧の直後にも「共通前提の直後に始まる最初の小問」と「選択肢（ア）〜（オ）」が含まれていた。参照・式番号・範囲表現は §4-1 の条件2・3で止めており、**日本語の語彙判定（助詞など）は使っていない。**

### v06 → v07

| 項目 | v06 | v07 |
|------|-----|-----|
| 表面の生成 | `Q` の本文をそのまま出力 | **小問の境界に `<br>` を挿入（§4-1）。入力形式・TSV構造・既存カードは不変** |

実運用で、1つの原問題に長い前提と複数の小問を含む `Q` が読みにくいことが分かった。実測では `Q` の表示量スコアが中央194・最大655で、**`A` の最大467を上回る**（通信工学42枚）。裏面は `A` 行ごとに `<br>` で分かれるのに表面だけ構造化されていなかったため、**表示処理側で解決した。** `question-prompt` の「`Q` は原文どおり」（§7-11-3）は変更していない。

### v01 → v02

旧仕様（半角セミコロン区切り・1行1カード）に由来する次の制約は、**すべて撤廃された**。

| 撤廃した制約 | 撤廃理由 |
|------------|---------|
| 本文に半角 `;` を書けない | 列区切りが存在しなくなった |
| 全角 `；` への置換 | 同上 |
| `\;` の禁止 | 同上 |
| HTML実体参照の禁止 | 同上（エスケープにより文字列として表示される） |
| 4列目以降の破棄 | 列が存在しなくなった |
| ` 出典：` の厳密一致 | `S` 行に置き換わった |
| 2列目で「出典」の語を使えない | 同上 |
| カード内改行の禁止 | `A` 行の複数化で表現する |
| `｜` の禁止 | 区切りとして使用しなくなった |
| `\[ ... \]` の禁止 | 実測により使用可能と確認された |

新たに追加された技術制約は **HTMLエスケープ（4-2）のみ**であり、これはツール内部の処理であるため、`question-prompt` 側に制約を生じない。

### v04 → v06

| 項目 | v04 | v06 |
|------|-----|-----|
| 保存形式 | 規定なし（実装が独断で `.txt`／`text/plain`） | **§4-4 として新設。`.tsv`／`text/tab-separated-values`** |
| 環境ごとの確認 | 規定なし | **PC・タブレット双方での確認を要件化** |
| 方式の選択 | ― | **選択させない。1方式に固定** |
| 実測記録 | ― | **3方式の比較結果を §4-4 に記録** |

（v05 は `application/octet-stream` を既定としたが、実測により `.txt` 付加が起きないと確認されたため、内容と一致する方式へ変更した。）

### v03 → v04

| 項目 | v03 | v04 |
|------|-----|-----|
| 表示量スコアの閾値 | 250（手書き6枚に基づく暫定値） | **500**（実測16枚に基づく） |
| 指標の性質 | 記載なし | **総量を測る指標であり読みやすさではないと明記** |
| 警告への対処 | 記載なし | **分割ではなく観点を絞ることと明記** |

### v02 → v03

テスト仕様の作成時に判明した未定義事項を解消した。

| 項目 | v02 | v03 |
|------|-----|-----|
| エラーと警告の判定原則 | 未記載 | **第5章として新設** |
| 検査項目の結果（7-4・8-5） | 検査するとだけ記載 | **すべて警告と明記** |
| マーカーがあるのに `S` が無い | エラー | **警告へ変更** |
| プレフィックス直後の空白が2個以上 | 未定義 | **正常（除去するだけ）** |
| プレフィックスの小文字 | 未定義 | **エラー（未定義の役割）** |
| 警告の一覧 | 2件のみ記載 | **11件を第10章に集約** |
